# <项目名> 架构总览

> 本文档是给 AI 开发者看的**索引**。用户决策记录见 `../total.md`。

## 技术栈

- 引擎：Unity <版本> + <渲染管线>
- 输入：<Input System / 旧 Input>
- 相机：<Cinemachine / 自写>
- UI：<UGUI / UI Toolkit>
- 数据：<ScriptableObject / 其他>
- 架构驱动：<如：命令模式 + 状态机 + 行为树>
- 表现：<如：事件总线 + FX/音效管理器>

## 模块清单

### 基础层（无依赖，可并行开发）
| 模块 | 内容 | 文档 |
|------|------|------|
| M1 | <模块名>：<核心职责> | `0X-xxx.md` |

### 业务层（依赖 M1/M2）
| 模块 | 内容 | 文档 |
|------|------|------|
| M3 | <模块名>：<核心职责> | `0X-xxx.md` |

### 控制层（依赖 M1）
| 模块 | 内容 | 文档 |
|------|------|------|
| M6 | <模块名>：<核心职责> | `0X-xxx.md` |

### 表现层（依赖业务层事件）
| 模块 | 内容 | 文档 |
|------|------|------|
| M12 | <模块名>：<核心职责> | `0X-xxx.md` |

### 集成层（依赖业务层）
| 模块 | 内容 | 文档 |
|------|------|------|
| M8 | <模块名>：<核心职责> | `0X-xxx.md` |

> 各层命名按项目性质调整（如无战斗系统则去掉战斗层）。

## 依赖图

```
M1 ──→ M2 ──→ M3 ──→ M4 ──→ M5
 │            │
 └──→ M6      └──→ M7

表现层 M8/M9 订阅事件总线
```

## 文档阅读指引

- **架构理解**：本文件（00-overview.md）→ 对应模块的 `0X-xxx.md`
- **验收测试**：实现后对照 `0X-xxx-test.md` 逐条验证
- **用户决策**：`../total.md`（用户视角，AI 不要当规范用）

## 文档索引表

| 任务类型 | 读这个 |
|---------|--------|
| <任务关键词，如：状态机/受击/闪避> | `Docs/architecture/0X-xxx.md` |
| <任务关键词，如：属性/数值/背包> | `Docs/architecture/0X-xxx.md` |
| <任务关键词，如：技能/冷却/消耗> | `Docs/architecture/0X-xxx.md` |
| 项目总览/模块依赖 | `Docs/architecture/00-overview.md` |
| 用户决策记录 | `Docs/total.md`（用户视角，AI 不当规范用） |

> 索引表用**任务关键词**（开发者会说的话）而非模块编号，便于快速匹配。
