# M<X> <模块名> 验收文档

> 对应架构文档：`0X-xxx.md`
> 验收方式：<自动化测试：EditMode 单测 + PlayMode 集成测试；或「不打自动化测试」。> + 在 Unity 中 Play 模式逐条验证
> 交付日期：<YYYY-MM-DD>

## 自动化测试

> 项目启用自动化测试时填写本表；未启用则整节删除。

### EditMode 单元测试（数值/纯逻辑，`Assets/Tests/EditMode/`）

| 测试类 | 用例 | 断言要点 | 预期 |
|--------|------|---------|------|
| `<被测类>Test.cs` | `<行为>_<预期>` | <如：概率和 ≤1、伤害非负> | PASS ☐ |
| `<被测类>Test.cs` | `<行为>_<预期>` | <如：切换后 CurrentState 是目标状态> | PASS ☐ |

### PlayMode 集成测试（状态转换/装配，`Assets/Tests/PlayMode/`）

| 测试类 | 用例 | 断言要点 | 预期 |
|--------|------|---------|------|
| `<系统>IntegrationTest.cs` | `<行为>_<预期>` | <如：场景装配无异常 + 关键事件触发> | PASS ☐ |

运行：Unity Editor → Window → General → Test Runner，全部 PASS 后进入手动验收。

## 前置条件

- 场景装配：<需要的 GameObject / 组件 / SO 配置>
- 依赖模块：<M<X> 已通过验收>

## 手动验收项（手感/表现，自动化覆盖不到的部分）

| # | 操作步骤 | 预期结果 | 通过 |
|---|---------|---------|------|
| 1 | <如：进入 Play，按 <键>> | <可观察现象，写具体> | ☐ |
| 2 | <操作> | <预期现象> | ☐ |
| 3 | <边界操作，如：连续快速触发> | <预期现象> | ☐ |

## 反例验证（容易漏掉的失败路径）

| # | 操作步骤 | 预期结果 | 通过 |
|---|---------|---------|------|
| 1 | <如：动画播放中再次触发> | <如：不应叠加/应正确处理> | ☐ |

## 回归项（修改本模块时需复验）

- [ ] M<X> <依赖本模块的功能> 仍正常
- [ ] <跨模块影响点>
- [ ] <项目有自动化测试时> 相关 EditMode/PlayMode 测试仍全 PASS

## 验收结论

- 自动化测试全部 PASS：☐ / 本模块无自动化测试：☐
- 手动验收全部通过：☐
- 未通过项与原因：<...>
